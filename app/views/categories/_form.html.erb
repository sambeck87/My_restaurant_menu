<%= form_with(model: category, url: category.persisted? ? restaurant_category_path(category.restaurant_id, category) : restaurant_categories_path) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.text_field :restaurant_id, value: params[:restaurant_id] %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
